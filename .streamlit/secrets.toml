# Streamlit Cloud Secrets Template
# Add these secrets in your Streamlit Cloud app settings under "Secrets"
# Go to: App dashboard → Settings → Secrets → Paste the content below

GEMINI_API_KEY = "your_gemini_api_key_here"
LLM_MODEL = "gemini-2.5-flash"
